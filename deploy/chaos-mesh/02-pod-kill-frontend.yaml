apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: kill-frontend
  namespace: chaos-testing
spec:
  action: pod-kill
  mode: all
  duration: 30s
  selector:
    namespaces:
      - default
    labelSelectors:
      app: frontend
